<h3>Greptile Summary</h3>

This PR fixes a crash in `pathd` triggered when `pathd_srte_policy_name_modify` was called with `NB_EV_VALIDATE` before the parent entry was present (the `nb_running_get_entry` abort flag was changed from `true` to `false` and a NULL guard was added for the VALIDATE path). It also adds a new topotest suite (`pathd_pcep_topo1`) with a Python PCE simulator and several broad enhancements across bgpd, ospfd, lib, and isisd.

- **pathd crash fix (`path_nb_config.c`)**: `nb_running_get_entry` is now non-aborting, and the VALIDATE branch properly null-checks `policy` before dereferencing `policy->name`.
- **New PCEP topotest**: introduces `pce_sim.py` (a minimal RFC 5440 PCE simulator) and `test_pathd_pcep_topo1.py` covering PCEP session bring-up, `pcep_pcc` binary exercise, message counter verification, and dead-timer disconnect paths.
- **BGP-LS MT-ID refactor**: flattens dynamic `uint16_t *` arrays to scalar `uint16_t` fields for link/prefix/SRv6-SID descriptors, adds proper attribute-level MT-ID encoding/decoding and frees, and wires MT-ID through ISIS-TE to BGP-LS origination.
- **OSPF RFC 4222 additions**: new per-interface `dead-timer-reset any-control` and `dscp all/low-control` VTY commands, with packet-handler hooks to reset the inactivity timer on non-Hello OSPF unicast packets.

<details><summary><h3>Confidence Score: 3/5</h3></summary>

The pathd crash fix is incomplete: the APPLY branch still unconditionally dereferences `policy` without a NULL guard, leaving pathd exposed to the same fault the fix intends to close.

The APPLY path in `pathd_srte_policy_name_modify` dereferences `policy` after `nb_running_get_entry(..., false)` without checking for NULL, so a mis-ordered or error-recovery transaction can still crash pathd. This issue was raised in the previous review and remains unaddressed. All other changes in the PR — BGP-LS MT-ID scalar refactor, OSPF RFC 4222 additions, the PCEP topotest, and the resolver callback fix — look well-structured with no additional correctness issues found.

`pathd/path_nb_config.c` needs the NULL guard extended to the APPLY branch. `bgpd/bgp_ls_nlri.c` has a minor style concern in `parse_node_msd`.
</details>


<details open><summary><h3>Important Files Changed</h3></summary>




| Filename | Overview |
|----------|----------|
| pathd/path_nb_config.c | Crash fix: nb_running_get_entry made non-aborting and VALIDATE branch null-guards policy; APPLY branch still dereferences policy without a NULL guard (flagged in prior review). |
| bgpd/bgp_ls_nlri.c | Large MT-ID refactor: dynamic arrays replaced with inline scalars; bgp_ls_attr_free confirmed to free attr->mt_id; parse_node_msd removes msd initializer (possible compiler warning). |
| tests/topotests/pathd_pcep_topo1/test_pathd_pcep_topo1.py | New PCEP topotest covering session bring-up, pcep_pcc binary, counter verification, and dead-timer teardown; correctly avoids the crashing counters CLI path. |
| tests/topotests/pathd_pcep_topo1/pce/pce_sim.py | New minimal RFC 5440 PCE simulator with correct Open/Keepalive encoding, per-connection threads, and structured logging for test assertions. |
| ospfd/ospf_packet.c | RFC 4222: inactivity-timer restart for non-Hello unicast/P2P-AllSPF packets; DSCP per-message-type selection; byte-order handling for dst address correctly uses ntohl. |
| ospfd/ospf_vty.c | New VTY commands for RFC 4222 DSCP knobs and dead-timer-reset any-control; dead_timer_resets counter displayed in neighbor detail. |
| lib/nexthop.c | Added brief parameter to nexthop_json_helper; vrf output moved before early-return so it is included in brief mode; all callers updated correctly. |
| lib/resolver.c | VRF-switch error path now invokes the callback with error args; callback pointer zeroed before invocation to prevent re-entrant calls. |
| bgpd/bgp_ls_ted.c | MT-ID wired through IS-IS TE to BGP-LS originate/withdraw for links, prefixes, and SRv6 SIDs; skips MT-ID==0 per RFC 9552 §5.2.2.1. |
| bgpd/bgp_nht.c | RFC 4724 §4.2: stale routes now deleted immediately when nexthop becomes unreachable (link down), preventing traffic blackholing. |
| isisd/isis_te.c | Tracks MT-ID through IS-IS LSP callbacks and populates LS_NODE_MT_IDS / LS_ATTR_MT_ID / LS_PREF_MT_ID in the Link State DB. |

</details>


</details>


<!-- greptile_other_comments_section -->

<details><summary>Prompt To Fix All With AI</summary>

`````markdown
Fix the following 2 code review issues. Work through them one at a time, proposing concise fixes.

---

### Issue 1 of 2
bgpd/bgp_ls_nlri.c:4107-4108
Removing the `= 255` initializer leaves `msd` technically uninitialized before the loop. The logic is correct — `msd` is always assigned via `msd = v` on the first `BGP_LS_IGP_MSD_TYPE_BASE_MPLS` hit before `MIN(msd, v)` is reached — but the compiler cannot easily prove this across loop iterations and may emit an "may be used uninitialized" warning in some build configurations. Restoring the zero-sentinel (or explicitly initializing to 0) removes the ambiguity without changing behavior.

```suggestion
	uint8_t msd = 0;
	bool had_msd = false;
```

### Issue 2 of 2
tests/topotests/pathd_pcep_topo1/pce/pce_sim.py:237
`open(pce_log_path, "a").close()` opens a file but never assigns it to a variable, so the file object is not explicitly closed — it relies on CPython's reference-counting GC to close it immediately. This is fine on CPython but is technically a resource-management anti-pattern. Consider `with open(pce_log_path, "a"): pass` or `pathlib.Path(pce_log_path).touch()` to make the intent clear.


`````

</details>

<sub>Reviews (3): Last reviewed commit: ["tests: Add a bit more testing to pathd/p..."](https://github.com/opensourcerouting/frr-greptile/commit/8ec686d4a6a05385e4e59b69053772fb2b01c354) | [Re-trigger Greptile](https://app.greptile.com/api/retrigger?id=32306583)</sub>
